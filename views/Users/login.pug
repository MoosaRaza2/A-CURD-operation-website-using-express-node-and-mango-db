extends ../layout

block content
  h1 Login
  form(action="/login",method="post")
        
        .form-group
            label(for="email") Enter Email
            input#email(type="text",class="form-control" name="email", value="rmoosa276@gmail.com")
        .form-group
            label(for="password") Enter password
            input#price(type="password",class="form-control" name="password",value="123")   
        input(type="submit", class="btn btn-info", value="Register")